import LoginButton from "./LoginButton";

const Login = () => {
  return (
    <section className="w-full h-full flex flex-col justify-center items-center">
      <h2 className="text-4xl text-white mb-4">React Firebase Chat</h2>
      <LoginButton />
    </section>
  );
};

export default Login;
